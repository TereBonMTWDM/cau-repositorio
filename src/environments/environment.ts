// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  FIREBASE: {
    apiKey: "AIzaSyA-DHSzO0rLLlSAokp5F7dzMOwyi0QxSyg",
    authDomain: "cau-repositorio.firebaseapp.com",
    databaseURL: "https://cau-repositorio.firebaseio.com",
    projectId: "cau-repositorio",
    storageBucket: "cau-repositorio.appspot.com",
    messagingSenderId: "893399350828",
    appId: "1:893399350828:web:82a3e654696c2dcec89b89"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
